services:
  meraki-exporter:
    build: ./perfScore
    image: meraki-exporter:local
    container_name: meraki-exporter
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
